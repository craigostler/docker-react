sudo: required
language: generic

services:
    - docker
    
before_install:
    - docker build -t craigostler/docker-react -f Dockerfile.dev .
    
script:
    - docker run -e CI=true craigostler/docker-react npm run test -- --coverage